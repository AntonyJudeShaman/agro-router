// import React, { FC, memo } from 'react';
// import MarkdownDisplay from 'react-native-markdown-display';

// export const MemoizedReactMarkdown: FC<{ children: string }> = memo(
//   ({ children }) => <MarkdownDisplay>{children}</MarkdownDisplay>,
//   () => true // Always return true to prevent re-renders since Markdown content is immutable
// );
